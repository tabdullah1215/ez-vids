# Copy this file and fill in real values:
#
#   For Vercel:  cp .env.example packages/api/.env.local
#   For Expo:    cp .env.example .env
#                (only EXPO_PUBLIC_* vars are used by Expo)

# ===== Creatify API (Vercel backend only) =====
CREATIFY_API_ID=
CREATIFY_API_KEY=
CREATIFY_BASE_URL=https://api.creatify.ai

# ===== Supabase =====
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# ^ NEVER put service_role key in the Expo app

# ===== Expo Public (safe to expose in mobile app) =====
EXPO_PUBLIC_API_URL=http://localhost:3000/api
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=


# =============================================================
# QUICK START CHECKLIST
# =============================================================
# 1. Sign up at https://app.creatify.ai → API plan → copy API ID + Key
# 2. Create Supabase project → copy URL + keys
# 3. Run SQL migration (supabase/migrations/001_create_video_jobs.sql)
# 4. Fill in this file → copy to packages/api/.env.local
# 5. cd packages/api && vercel dev
# 6. Test: curl http://localhost:3000/api/health
# 7. npx expo start (from project root)
# 8. Tap "Test API Connection" → then "Generate Video"
