# Copy this file and fill in real values:
#
#   For Edge Functions:  cp .env.example supabase/functions/.env
#   For Expo:            cp .env.example .env
#                        (only EXPO_PUBLIC_* vars are used by Expo)

# ===== Creatify API (Edge Functions only — never expose to Expo) =====
CREATIFY_API_ID=
CREATIFY_API_KEY=
CREATIFY_BASE_URL=https://api.creatify.ai

# ===== Supabase (Edge Functions only — service role is secret) =====
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# ^ NEVER put service_role key in the Expo app

# ===== Expo Public (safe to expose in mobile app) =====
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=


# =============================================================
# QUICK START CHECKLIST
# =============================================================
# 1. Sign up at https://app.creatify.ai → API plan → copy API ID + Key
# 2. Create Supabase project → copy URL + keys
# 3. Run SQL migration (supabase/migrations/001_create_video_jobs.sql)
# 4. Fill in this file → copy to supabase/functions/.env
# 5. supabase functions serve   (or deploy to cloud)
# 6. Test: curl https://<project>.supabase.co/functions/v1/health \
#       -H "Authorization: Bearer <anon-key>"
# 7. Fill EXPO_PUBLIC_* → copy to .env
# 8. npx expo start
# 9. Tap "Test API Connection" → then "Generate Video"
